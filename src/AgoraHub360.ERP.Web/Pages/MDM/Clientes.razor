@page "/mdm/clientes"
@attribute [Authorize]
@inject ClienteHttpService ClienteSvc

<PageTitle>Clientes - AgoraHub360 ERP</PageTitle>

<div class="page-header">
    <h1>Clientes</h1>
    <p class="text-muted mb-0">Datos Maestros &rsaquo; Clientes</p>
</div>

@if (!string.IsNullOrEmpty(_mensaje))
{
    <div class="alert @(_esError ? "alert-danger" : "alert-success") alert-dismissible fade show">
        @_mensaje
        <button type="button" class="btn-close" @onclick="() => _mensaje = null"></button>
    </div>
}

<div class="card mb-3">
    <div class="card-header d-flex justify-content-between align-items-center py-3">
        <div class="d-flex align-items-center gap-3">
            <span>Clientes (@ItemsFiltrados.Count)</span>
            <input type="text" class="form-control form-control-sm" placeholder="Buscar..."
                   style="width: 220px;" @bind="_search" @bind:event="oninput" />
        </div>
        <button class="btn btn-primary btn-sm" @onclick="ShowCrear">
            <i class="bi bi-plus-circle me-1"></i> Nuevo Cliente
        </button>
    </div>

    @if (_loading)
    {
        <div class="card-body text-center py-5">
            <div class="spinner-border text-primary"></div>
        </div>
    }
    else if (_items.Count == 0)
    {
        <div class="card-body text-center py-5">
            <i class="bi bi-people" style="font-size: 3rem; color: var(--agora-text-light);"></i>
            <p class="text-muted mt-3">No hay clientes registrados.</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover mb-0">
                <thead>
                    <tr>
                        <th>Codigo</th>
                        <th>Razon Social</th>
                        <th>NIT</th>
                        <th>Telefono</th>
                        <th>Email</th>
                        <th>Tipo</th>
                        <th class="text-center">Estado</th>
                        <th class="text-end">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in ItemsPaginados)
                    {
                        <tr>
                            <td class="fw-semibold">@item.Codigo</td>
                            <td>@item.RazonSocial</td>
                            <td>@(item.NIT ?? "—")</td>
                            <td>@(item.Telefono ?? "—")</td>
                            <td>@(item.Email ?? "—")</td>
                            <td><span class="badge bg-info">@item.TipoCliente</span></td>
                            <td class="text-center">
                                <span class="badge @(item.Activo ? "bg-success" : "bg-secondary")">
                                    @(item.Activo ? "Activo" : "Inactivo")
                                </span>
                            </td>
                            <td class="text-end">
                                <button class="btn btn-outline-primary btn-sm me-1"
                                        @onclick="() => ShowEditar(item)" title="Editar">
                                    <i class="bi bi-pencil"></i>
                                </button>
                                <button class="btn btn-outline-danger btn-sm"
                                        @onclick="() => ConfirmDelete(item)" title="Eliminar">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (TotalPaginas > 1)
        {
            <div class="card-footer d-flex justify-content-between align-items-center py-2">
                <small class="text-muted">Mostrando @ItemsPaginados.Count de @ItemsFiltrados.Count</small>
                <nav>
                    <ul class="pagination pagination-sm mb-0">
                        <li class="page-item @(_currentPage <= 1 ? "disabled" : "")">
                            <button class="page-link" @onclick="() => _currentPage--">&laquo;</button>
                        </li>
                        @for (var p = 1; p <= TotalPaginas; p++)
                        {
                            var page = p;
                            <li class="page-item @(page == _currentPage ? "active" : "")">
                                <button class="page-link" @onclick="() => _currentPage = page">@page</button>
                            </li>
                        }
                        <li class="page-item @(_currentPage >= TotalPaginas ? "disabled" : "")">
                            <button class="page-link" @onclick="() => _currentPage++">&raquo;</button>
                        </li>
                    </ul>
                </nav>
            </div>
        }
    }
</div>

<!-- Modal Crear/Editar -->
@if (_showForm)
{
    <div class="modal fade show d-block" tabindex="-1" style="background: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">@(_editId.HasValue ? "Editar Cliente" : "Nuevo Cliente")</h5>
                    <button type="button" class="btn-close" @onclick="CloseForm"></button>
                </div>
                <EditForm Model="_formModel" OnValidSubmit="SaveAsync">
                    <DataAnnotationsValidator />
                    <div class="modal-body">
                        <div class="row mb-3">
                            <div class="col-md-4">
                                <label class="form-label">Codigo *</label>
                                <InputText class="form-control" @bind-Value="_formModel.Codigo" />
                                <ValidationMessage For="() => _formModel.Codigo" />
                            </div>
                            <div class="col-md-8">
                                <label class="form-label">Razon Social *</label>
                                <InputText class="form-control" @bind-Value="_formModel.RazonSocial" />
                                <ValidationMessage For="() => _formModel.RazonSocial" />
                            </div>
                        </div>
                        <div class="row mb-3">
                            <div class="col-md-4">
                                <label class="form-label">NIT</label>
                                <InputText class="form-control" @bind-Value="_formModel.NIT" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Telefono</label>
                                <InputText class="form-control" @bind-Value="_formModel.Telefono" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Email</label>
                                <InputText class="form-control" @bind-Value="_formModel.Email" />
                                <ValidationMessage For="() => _formModel.Email" />
                            </div>
                        </div>
                        <div class="row mb-3">
                            <div class="col-md-8">
                                <label class="form-label">Direccion</label>
                                <InputText class="form-control" @bind-Value="_formModel.Direccion" />
                            </div>
                            <div class="col-md-4">
                                <label class="form-label">Tipo Cliente *</label>
                                <InputSelect class="form-select" @bind-Value="_formModel.TipoCliente">
                                    <option value="General">General</option>
                                    <option value="Mayorista">Mayorista</option>
                                    <option value="Minorista">Minorista</option>
                                    <option value="Corporativo">Corporativo</option>
                                </InputSelect>
                            </div>
                        </div>
                        <div class="mb-3">
                            <label class="form-label">Nombre Contacto</label>
                            <InputText class="form-control" @bind-Value="_formModel.NombreContacto" />
                        </div>
                        @if (_editId.HasValue)
                        {
                            <div class="form-check">
                                <InputCheckbox class="form-check-input" @bind-Value="_formActivo" />
                                <label class="form-check-label">Activo</label>
                            </div>
                        }
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" @onclick="CloseForm">Cancelar</button>
                        <button type="submit" class="btn btn-primary" disabled="@_saving">
                            @if (_saving) { <span class="spinner-border spinner-border-sm me-1"></span> }
                            @(_editId.HasValue ? "Guardar" : "Crear Cliente")
                        </button>
                    </div>
                </EditForm>
            </div>
        </div>
    </div>
}

<!-- Modal Eliminar -->
@if (_deleteTarget is not null)
{
    <div class="modal fade show d-block" tabindex="-1" style="background: rgba(0,0,0,0.5);">
        <div class="modal-dialog modal-sm">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirmar</h5>
                    <button type="button" class="btn-close" @onclick="() => _deleteTarget = null"></button>
                </div>
                <div class="modal-body">
                    <p>Eliminar al cliente <strong>@_deleteTarget.RazonSocial</strong>?</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-secondary btn-sm" @onclick="() => _deleteTarget = null">Cancelar</button>
                    <button class="btn btn-danger btn-sm" @onclick="DeleteAsync" disabled="@_saving">Eliminar</button>
                </div>
            </div>
        </div>
    </div>
}

@code {
    private List<ClienteDto> _items = new();
    private bool _loading = true, _saving;
    private string? _mensaje;
    private bool _esError;

    private bool _showForm;
    private int? _editId;
    private CreateClienteDto _formModel = new();
    private bool _formActivo = true;
    private ClienteDto? _deleteTarget;

    private string _search = string.Empty;
    private int _currentPage = 1;
    private int _pageSize = 15;

    private List<ClienteDto> ItemsFiltrados => _items
        .Where(i => string.IsNullOrEmpty(_search)
            || i.Codigo.Contains(_search, StringComparison.OrdinalIgnoreCase)
            || i.RazonSocial.Contains(_search, StringComparison.OrdinalIgnoreCase)
            || (i.NIT ?? "").Contains(_search, StringComparison.OrdinalIgnoreCase))
        .ToList();

    private List<ClienteDto> ItemsPaginados => ItemsFiltrados
        .Skip((_currentPage - 1) * _pageSize)
        .Take(_pageSize)
        .ToList();

    private int TotalPaginas => (int)Math.Ceiling(ItemsFiltrados.Count / (double)_pageSize);

    protected override async Task OnInitializedAsync()
    {
        await LoadDataAsync();
    }

    private async Task LoadDataAsync()
    {
        _loading = true;
        try { _items = await ClienteSvc.GetAllAsync(); }
        catch { _mensaje = "Error al cargar clientes."; _esError = true; }
        _loading = false;
    }

    private void ShowCrear()
    {
        _editId = null;
        _formModel = new CreateClienteDto();
        _formActivo = true;
        _showForm = true;
    }

    private void ShowEditar(ClienteDto item)
    {
        _editId = item.Id;
        _formModel = new CreateClienteDto
        {
            Codigo = item.Codigo,
            RazonSocial = item.RazonSocial,
            NIT = item.NIT,
            Direccion = item.Direccion,
            Telefono = item.Telefono,
            Email = item.Email,
            NombreContacto = item.NombreContacto,
            TipoCliente = item.TipoCliente
        };
        _formActivo = item.Activo;
        _showForm = true;
    }

    private void CloseForm() { _showForm = false; _editId = null; }

    private async Task SaveAsync()
    {
        _saving = true;
        try
        {
            if (_editId.HasValue)
            {
                var dto = new UpdateClienteDto
                {
                    Codigo = _formModel.Codigo,
                    RazonSocial = _formModel.RazonSocial,
                    NIT = _formModel.NIT,
                    Direccion = _formModel.Direccion,
                    Telefono = _formModel.Telefono,
                    Email = _formModel.Email,
                    NombreContacto = _formModel.NombreContacto,
                    TipoCliente = _formModel.TipoCliente,
                    Activo = _formActivo
                };
                var r = await ClienteSvc.UpdateAsync(_editId.Value, dto);
                if (!r.Success) { _mensaje = r.Message; _esError = true; _saving = false; return; }
                _mensaje = "Cliente actualizado."; _esError = false;
            }
            else
            {
                var r = await ClienteSvc.CreateAsync(_formModel);
                if (!r.Success) { _mensaje = r.Message; _esError = true; _saving = false; return; }
                _mensaje = "Cliente creado."; _esError = false;
            }
            CloseForm();
            await LoadDataAsync();
        }
        catch { _mensaje = "Error de comunicacion."; _esError = true; }
        _saving = false;
    }

    private void ConfirmDelete(ClienteDto item) => _deleteTarget = item;

    private async Task DeleteAsync()
    {
        if (_deleteTarget is null) return;
        _saving = true;
        try
        {
            var r = await ClienteSvc.DeleteAsync(_deleteTarget.Id);
            _mensaje = r.Success ? "Cliente eliminado." : r.Message;
            _esError = !r.Success;
            _deleteTarget = null;
            await LoadDataAsync();
        }
        catch { _mensaje = "Error de comunicacion."; _esError = true; }
        _saving = false;
    }
}
